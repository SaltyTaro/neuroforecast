This final selection sprint was authorized on September 20, 2026 IST. It starts from public measured functional responses. The earlier DosePilot and OxyGuard outcomes remain unchanged. Their 15% and 20% gates are historical internal criteria, not requirements imposed by the competition.

The audit is limited to three dataset candidates: (1) DeePhys, including its acute quinpirole experiment; (2) the TUNI comparative human/rat MEA release, including matched pharmacology recordings; and (3) the EPA rat cortical neuronal network formation assay, whose data record explicitly lists raw measurements across development for 136 unique compounds and ten repeat experiments. Related papers and metadata may be inspected to assess prior work and data interpretation, without expanding this into an open-ended topic search.

For each candidate, verify downloadable payloads, data license, the experimental unit and repeat structure, input/target timing, available measured endpoints, and the closest prior analysis. File size, neuron counts, electrode counts, and time windows must not be substituted for independent biological replicates. A method that already exists in the source paper is a comparator, not a new contribution.

Select a narrow user decision that can be evaluated against inexpensive strong baselines. Freeze a separate numerical protocol after the data schema and endpoint semantics are understood but before evaluating candidate prediction methods. Forecasts must use only measurements available at their stated cutoff. A prospective holdout must remain untouched until the method is locked; exploratory selection results must be identified as such.

Practical utility may be demonstrated by a supported improvement in error, missed-effect rate, cost, or reliability. There is no universal percentage cutoff. Any numerical target must be justified for the selected decision, and simple comparators and ablations must receive the same inputs and evaluation units. Small-sample uncertainty and incomplete experimental metadata are decision-relevant limitations.

The intended selection package is one concrete question, verified data inventory, baseline comparison, prior-work boundary, and a development recommendation. A product interface, clinical claim, living-chip validation claim, or submission is outside this selection step. All new research data remain under `E:\kaggle\AI4S\raw\research\round3`; all numerical experiment artifacts will be under `E:\kaggle\AI4S\experiments`.
